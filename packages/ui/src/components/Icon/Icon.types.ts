export interface IconProps {

  /**
   * The name of the icon to render, using the Iconify naming convention (for example, `"mdi:home"`).
   * See the [Iconify documentation](https://iconify.design/docs/).
   */
  name: string
}
